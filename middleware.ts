export { default } from "next-auth/middleware"

export const config = {
  matcher: [
    // '/pre-booking/[id]',
    // '/pre-booking/[id]/payment',
    // '/pre-booking/success', 
    // '/dashboard',
    // '/booking',
    // '/dashboard/add-property', 
    // '/calendar',
    // '/hunt-cash',
    // '/account',
    // '/account/settings',   
    // '/account/inbox',    
  ],
}
